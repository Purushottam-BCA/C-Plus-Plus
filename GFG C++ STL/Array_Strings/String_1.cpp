#define ar(i,n) for(int i=0;i<n;i++)
#define ard(i,n) for(int i=n-1;i>=0;--i)
#define rep(i,a,b) for(int i=a;i<=b;++i)
#define nl cout<<"\n"
#define len(a) sizeof(a)/sizeof(a[0])

#include<bits/stdc++.h>
using namespace std;

int main()
{
   string a;
  /* a="ABACPQBCGHIJKLMNO";
   cout<<"Length : "<<a.length();
   nl;
   a+=" India";
   cout<<"Concatenation : "<<a;
   nl;
   cout<<"SUBSTRING : "<<a.substr(2,3);
   nl;
   cout<<"FINDING : "<<a.find("BC");
   nl;*/

  /*  // Comparision of strings
  a="ABC";
   string b="abc";
   if(a==b)
      cout<<"Same";
   else if(a<b)
      cout<<"1st is Smaller";
   else
      cout<<"1st is bigger"; */

   /* // Reading with spaces
   cout<<"Enter Name : ";
   getline(cin,a,'$');
   nl;
   cout<<"Your Name is : "<<a;
   nl;
   */
   return 0;
}
