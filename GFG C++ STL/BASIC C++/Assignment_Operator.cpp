#include<bits/stdc++.h>
using namespace std;

int main()
{
   // cout<<(0.0/0.0);
   int a=10,b=20;
/*  Available in C++20 only
   auto m = a<=>b;
   if(m<0)
      cout<<"X is less";
   else if(m>0)
      cout<<"x is more ";
   else
      cout<<"X = Y"; */
      cout<<std::noboolalpha;
      cout<<(5>2 and 8<7);
   return 0;
}
