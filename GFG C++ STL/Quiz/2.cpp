/*

#define hypo(a,b) sqrt(a*a+b*b)

what will it do ? hypo(a+2,b+3);

Ans :- It will replace as

   hypo( a , b ) = sqrt( a * a + b * b )
   hypo(a+2,b+3) = sqrt(a+2*a+2+b+3*b+3)
                 = sqrt(a+ 2a+2+b+ 3b+3)
                 = sqrt(3a+4b+5)

*/
